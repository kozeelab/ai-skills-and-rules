---
name: "verification-before-completion"
displayName: "完成前验证"
description: "在声称工作完成之前，必须运行验证命令并确认输出。证据先于断言，永远如此"
version: "1.0.0"

author:
  name: "kozee"
  url: "https://github.com/kozeelab"

category: "development"
tags:
  - "验证"
  - "完成检查"
  - "质量保障"
  - "证据驱动"
  - "工作流"

compatibility:
  platforms:
    - "linux"
    - "macos"
    - "windows"
  languages:
    - "any"

workflow:
  triggers:
    - "即将声称工作完成"
    - "即将提交代码"
    - "即将创建 PR"
    - "即将声称 Bug 已修复"
  next_skills: []
  priority: 10

input:
  description: "要验证的工作内容"
  required:
    - name: "work_description"
      type: "string"
      description: "要验证的工作描述"
  optional:
    - name: "verification_commands"
      type: "array"
      description: "验证命令列表"
      default: "auto-detect"

output:
  description: "验证结果报告"
  artifacts:
    - name: "verification_report"
      type: "string"
      description: "验证结果，包含命令输出和状态"
---

# 完成前验证

## 功能说明

> **核心目标**：在没有验证的情况下声称工作完成，是不诚实，不是高效。**证据先于断言，永远如此。**

**违反此规则的字面意思就是违反此规则的精神。**

---

## 铁律

```
没有新鲜的验证证据 → 不声称完成
```

如果你在这条消息中没有运行验证命令，你**不能**声称它通过了。

---

## 门禁函数

```
在声称任何状态或表达满意之前：

1. 识别：什么命令能证明这个声称？
2. 运行：执行完整命令（新鲜的、完整的）
3. 阅读：完整输出，检查退出码，计数失败
4. 验证：输出是否确认了声称？
   - 如果否：陈述实际状态并附上证据
   - 如果是：陈述声称并附上证据
5. 然后才：做出声称

跳过任何步骤 = 撒谎，不是验证
```

---

## 常见失败

| 声称 | 需要 | 不够 |
|------|------|------|
| 测试通过 | 测试命令输出：0 失败 | 之前的运行、"应该通过" |
| Linter 干净 | Linter 输出：0 错误 | 部分检查、推断 |
| 构建成功 | 构建命令：exit 0 | Linter 通过、日志看起来没问题 |
| Bug 已修复 | 测试原始症状：通过 | 代码改了、假设修好了 |
| 回归测试有效 | RED-GREEN 循环已验证 | 测试通过一次 |
| 需求满足 | 逐行检查清单 | 测试通过 |

---

## 危险信号 — 立即停下

- 使用"应该"、"大概"、"看起来"
- 在验证之前表达满意（"太好了！"、"完美！"、"搞定！"等）
- 即将提交/推送/PR 但没有验证
- 依赖部分验证
- 想着"就这一次"
- 累了想赶紧结束
- **任何暗示成功但没有运行验证的措辞**

---

## 常见借口反驳表

| 借口 | 现实 |
|------|------|
| "应该没问题了" | **运行**验证命令 |
| "我很有信心" | 信心 ≠ 证据 |
| "就这一次" | 没有例外 |
| "Linter 通过了" | Linter ≠ 编译器 ≠ 测试 |
| "子代理说成功了" | 独立验证 |
| "我累了" | 疲劳 ≠ 借口 |
| "部分检查够了" | 部分什么都证明不了 |
| "换个说法所以规则不适用" | 精神高于字面 |

---

## 关键模式

### 测试验证

```
✅ [运行测试命令] [看到：34/34 通过] "所有测试通过"
❌ "应该通过了" / "看起来正确"
```

### 回归测试（TDD RED-GREEN）

```
✅ 写测试 → 运行（通过）→ 撤销修复 → 运行（必须失败）→ 恢复 → 运行（通过）
❌ "我写了回归测试"（没有 RED-GREEN 验证）
```

### 构建验证

```
✅ [运行构建] [看到：exit 0] "构建通过"
❌ "Linter 通过了"（Linter 不检查编译）
```

### 需求验证

```
✅ 重读计划 → 创建检查清单 → 逐项验证 → 报告差距或完成
❌ "测试通过了，阶段完成"
```

---

## 何时应用

**始终在以下之前：**
- 任何成功/完成声称的变体
- 任何满意的表达
- 任何关于工作状态的正面陈述
- 提交、PR 创建、任务完成
- 移动到下一个任务

---

## 底线

**验证没有捷径。**

运行命令。阅读输出。**然后**声称结果。

这是不可协商的。

---

## 与其他 Skill 的关系

| 关系 | Skill | 说明 |
|------|-------|------|
| **前置** | `test-driven-development` | TDD 完成后进行最终验证 |
| **前置** | `systematic-debugging` | 修复后验证确实有效 |
| **前置** | `subagent-driven-development` | 所有任务完成后最终验证 |
| **协作** | `code-review-auto-fix` | 审查通过后验证代码确实能运行 |
